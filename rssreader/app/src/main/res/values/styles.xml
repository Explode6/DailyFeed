<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <!--    RssSourceActivity的主题-->
    <style name="RssSourceTheme" parent="AppTheme">
        <!--        设置沉浸式状态栏-->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!--        状态栏文字和图标颜色改为灰色-->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="bottomWindowAnim" parent="android:Animation">
        <item name="android:windowExitAnimation">@anim/bottom_exit_anim</item><!-- 退出动画-->
        <item name="android:windowEnterAnimation">@anim/bottom_enter_anim</item><!--- 进入动画-->
    </style>

</resources>
